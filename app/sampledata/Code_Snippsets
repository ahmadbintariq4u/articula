// Bitmap image return
if (cur.moveToFirst()){
            byte[] imgByte = cur.getBlob(6);
            cur.close();
            return BitmapFactory.decodeByteArray(imgByte, 0, imgByte.length);
        }
